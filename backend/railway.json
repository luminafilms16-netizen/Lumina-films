{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --no-audit && npm install --os=linux --cpu=x64 --libc=glibc sharp"
  },
  "deploy": {
    "startCommand": "node server.js"
  }
}